name: Release SwanLab Skill

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: read

concurrency:
  group: release-swanlab-skill-main
  cancel-in-progress: false

jobs:
  release:
    name: Package and release
    runs-on: ubuntu-slim

    steps:
      - name: Confirm a pull request was merged
        id: trigger
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          EVENT_NAME: ${{ github.event_name }}
          TARGET_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail

          if [[ "$EVENT_NAME" == "workflow_dispatch" ]]; then
            echo "release=true" >> "$GITHUB_OUTPUT"
            echo "Manual release requested for ${TARGET_SHA}."
            exit 0
          fi

          pr_number="$(
            gh api \
              -H "Accept: application/vnd.github+json" \
              "repos/${GITHUB_REPOSITORY}/commits/${TARGET_SHA}/pulls" \
              --jq 'map(select(.merged_at != null and .base.ref == "main")) | .[0].number // empty'
          )"

          if [[ -z "$pr_number" ]]; then
            echo "release=false" >> "$GITHUB_OUTPUT"
            echo "Push ${TARGET_SHA} is not associated with a merged pull request; skipping release."
            exit 0
          fi

          {
            echo "release=true"
            echo "pr_number=${pr_number}"
          } >> "$GITHUB_OUTPUT"
          echo "Detected merged pull request #${pr_number}."

      - name: Check out the merged commit
        if: steps.trigger.outputs.release == 'true'
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          persist-credentials: false

      - name: Read the Skill version
        if: steps.trigger.outputs.release == 'true'
        id: version
        shell: bash
        run: |
          set -euo pipefail

          version="$(
            sed -n 's/^  version: "\([^"]*\)"[[:space:]]*$/\1/p' skills/swanlab-skill/SKILL.md
          )"

          if [[ -z "$version" || "$version" == *$'\n'* ]]; then
            echo "::error::Expected exactly one metadata.version in skills/swanlab-skill/SKILL.md."
            exit 1
          fi

          if [[ ! "$version" =~ ^[0-9][0-9A-Za-z._-]*$ ]]; then
            echo "::error::Unsupported Skill version: ${version}"
            exit 1
          fi

          if ! grep -Fq "Skill-v${version}-" README.md; then
            echo "::error::README Skill badge does not match metadata.version ${version}."
            exit 1
          fi

          {
            echo "version=${version}"
            echo "tag=v${version}"
            echo "archive_base=swanlab-skill-v${version}"
          } >> "$GITHUB_OUTPUT"

      - name: Skip when the release tag already exists
        if: steps.trigger.outputs.release == 'true'
        id: publish
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.version.outputs.tag }}
        run: |
          set -euo pipefail

          if gh api \
            -H "Accept: application/vnd.github+json" \
            "repos/${GITHUB_REPOSITORY}/git/ref/tags/${TAG}" \
            >/dev/null 2>&1; then
            echo "release=false" >> "$GITHUB_OUTPUT"
            echo "Tag ${TAG} already exists; skipping release."
            exit 0
          fi

          echo "release=true" >> "$GITHUB_OUTPUT"
          echo "Tag ${TAG} is new; continuing with release."

      - name: Package the Skill
        if: steps.publish.outputs.release == 'true'
        run: make package

      - name: Verify and prepare release assets
        if: steps.publish.outputs.release == 'true'
        shell: bash
        env:
          ARCHIVE_BASE: ${{ steps.version.outputs.archive_base }}
        run: |
          set -euo pipefail

          unzip -tq dist/swanlab-skill.zip
          unzip -Z1 dist/swanlab-skill.zip > "$RUNNER_TEMP/swanlab-skill-zip-files.txt"

          grep -Fxq 'SKILL.md' "$RUNNER_TEMP/swanlab-skill-zip-files.txt"

          if grep -Eq '(^|/)(__pycache__/|[^/]*\.pyc$)' \
            "$RUNNER_TEMP/swanlab-skill-zip-files.txt"; then
            echo "::error::Python cache files were found in a release archive."
            exit 1
          fi

          mv dist/swanlab-skill.zip "dist/${ARCHIVE_BASE}.zip"

      - name: Create the GitHub Release
        if: steps.publish.outputs.release == 'true'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.version.outputs.tag }}
          ARCHIVE_BASE: ${{ steps.version.outputs.archive_base }}
        run: |
          set -euo pipefail

          gh release create "$TAG" \
            "dist/${ARCHIVE_BASE}.zip#SwanLab Skill zip" \
            --target "$GITHUB_SHA" \
            --title "swanlab-skill-${TAG}" \
            --generate-notes \
            --fail-on-no-commits

          release_url="$(gh release view "$TAG" --json url --jq .url)"
          {
            echo "## swanlab-skill-${TAG}"
            echo
            echo "Release: ${release_url}"
          } >> "$GITHUB_STEP_SUMMARY"
